{
  "scripts": {
    "scripts": {
      "compile": [
        "bower install",
        "gulp build"
      ]
    },
    "post-update-cmd": "bower install",
    "post-update-cmd": "gulp build"
  },
  "require": {
    "php": ">=5.3.0"
  }
}